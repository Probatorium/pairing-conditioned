# Rigour report, session two

Session identifier: S002. Date: 2026-08-11. Scope as instructed: name, remote
and contracts.

No quantity concerning the family of the preregistration was computed. The
lane still has measured nothing. The received King Wen sequence entered the
repository, which is loading and not measuring, and the one precondition that
could stop the lane was deliberately not run, because it belongs to the
analysis.

Following the practice that came out of defect three: this report enumerates
the commits of the session, and the commit that carries this report is not
among them.

---

## Opening state

Head `43319e2`, working tree clean, all gates passing, both hooks installed
and current. Verified before anything was changed.

## Recorded against session one, without reopening it

The rigour report of session one counted five commits and did not count the
commit that carried the report itself, so a reader standing at the head of
that session sees six. The enumeration in that report is correct; only the
total is short, and only by one. Session one stays closed and its report
stays as written. Recorded in the effort log of this session and as defect
three.

## The decisions

All seven pending questions answered by Alexis and recorded in
`DECISIONS.md` with decider and date, plus two more taken during the
publication procedure on findings that procedure produced.

| decision | what changed in the repository |
| --- | --- |
| one, the name | `NAME-GATE.md` gains a decision section; the measurement and the verdict that argued for it are kept unrewritten |
| two, publication | the house procedure was built, run, and its finding referred upward before any push |
| three, English | ratified, nothing corrected backwards |
| four, the design | recorded as deliberate, with the decomposition named as where the open tests live and a binding instruction for the analysis document |
| five, the source | the sequence received, transcribed mechanically, marked never recomputed, manifest entry with digest |
| six, the figure gate | identifier patterns with reasons and counts, registry outranking every pattern |
| seven, the untouchable lane | contact rule four, by repository path, with the citation ban written out and made executable |
| eight, the swept residue | the account name in the superseded blob accepted and declared, as defect four |
| nine, the push target | pushed to the address that exists, since the rename had not been made |

## Commits of this session

Four, not counting the commit that carries this report.

- `fce9b05` the decisions, the contracts they change, and the gates they need
- `aab95b0` the received King Wen sequence, transcribed at the deposit tag
- `154bae9` the pre publication sweep, and what it found
- `749e257` the two decisions the publication procedure produced

## State of the chain

The effort log verifies: chain intact, every entry recomputing to its stored
digest, session structure sound. Nine entries were added in this session and
the session is closed by the last of them. The head digest at close is
recorded in that closing entry.

## Dash sweep

Working tree, every tracked text file: clean. Every blob of every commit:
clean. The gate still holds no exemption for its own source, and the
exemption it does hold is exercised by the tests in both directions.

## Figure gate

The registry now holds seven figures. Every one of them is still
`cited-unverified`, so the number of figures usable in a commit message is
still zero, and every commit message of this session was written without an
accountable numeral.

The gate now distinguishes an identifier from a measurement, under the three
conditions of decision six. The condition that carries the weight is the
third: a token in the registry can never be exempted by any pattern. It is
tested directly, with a message where a year passes and a registered figure
that also looks like a year does not.

## Untouchable gate

Passes across tree, remotes, config and every blob of every commit.

It was tested by breaking it: a plain mention of an untouchable repository in
an ordinary file, and a mention of another in the reference list. It refused
both and refused the second differently and more loudly, because naming an
unpublished manuscript in a reference list is the citation the rule exists to
prevent. The test files were removed and the gate returned to passing.

## The received sequence

From `github.com/Probatorium/kingwen-orderings-replication` at `zenodo-v3`,
file `verify_paper.py`, symbol `KING_WEN`, retrieved 2026-08-11, read only,
with the neighbour confirmed still on its own branch afterwards. Digest in
`THIRD-PARTY-MANIFEST.md`.

Transcribed mechanically by `tools/receive_kingwen.py`, not typed, so a
copying error is not one of the ways this lane can be wrong. Checked on entry
for length, distinctness and range. Marked as received data, never recomputed.
The corroboration against the appendix of the cited theorem is recorded as
received from the prior lane and explicitly not performed here.

The local checkout path is passed on the command line and is stored in no
file.

## Publication

The house procedure, all three steps, in order.

1. One line log of every commit: run, eight commits at that point, listed
   oldest first.
2. Sweep of every blob of every commit: **it found something.** The session
   one version of `CONTACT-RULES.md`, in commit `59283f7`, names neighbouring
   repositories by their local checkout directory, and two of those lines
   carry the machine account name. The working tree was already clean, having
   been corrected forward earlier in the session for an independent reason.
   Referred to Alexis rather than resolved here. Accepted and declared, as
   decision eight and defect four, with the cost written out: that account
   name is public in the history and stays public.
3. `git ls-remote`: the remote held nothing, confirmed immediately before the
   push.

After the push:

| | |
| --- | --- |
| address | `https://github.com/Probatorium/pairing-conditioned` |
| visibility | PUBLIC |
| local head | `749e257471a8d6c4bddc71fde78108288c153206` |
| remote head | `749e257471a8d6c4bddc71fde78108288c153206` |
| local tree | `d951e89274ea46e9d53f5e82dc56ce4bba0fbae4` |
| remote tree | `d951e89274ea46e9d53f5e82dc56ce4bba0fbae4` |
| difference between the trees | none, `git diff` against the fetched remote is empty |
| commits held locally and absent on the remote | none |

The preregistration now carries a public timestamp, which is the property it
was written to have.

## The finding that matters more than any of the above

While locating the received sequence, the deposit it came from was seen to
carry an appendix titled, in its own code, a ladder of conditional nulls, and
a deposited sibling repository of that name exists.

That is a conditional null construction on the King Wen sequence, by the same
author, already deposited. The question of this lane is where an observed
count falls inside a conditional null. **This lane does not know whether
those are the same question, because it has not read them, and it did not
read them in this session on purpose.**

It is recorded at the top of `BACKGROUND-REVIEW.md` as the first thing that
review must settle. Until somebody reads them, this lane asserts nothing
about novelty in either direction, and the possibility that it is asking a
question its own author has already answered stays on the record rather than
being managed away.

## What is pending, and belongs to Alexis

1. **The rename on the forge.** Not made when this session ran. When it is,
   the remote address is adjusted and verified, and that is a one command
   step. The adopted name is already `matching-conditioned` in every artefact
   that names it.
2. **The local directory.** Still carries the old name. Cosmetic, and left
   alone rather than changed under a running session.
3. **The background review.** The recommendation of this session, and the
   only recommendation it makes: open it as session three, before any
   analysis. The lead above could change what this lane is for, and running
   the declared design first would mean discovering that afterwards.
4. **Deposit and tagging.** Not authorised, not done, not requested. The
   authorisation given was for this push.

## What is not claimed

Nothing about design, intention or ancient knowledge. No novelty of anything,
in either direction, while the background review is unstarted. Nothing from
the unpublished lane is cited, and the gate that enforces that runs on every
commit.
